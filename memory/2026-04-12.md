# 2026-04-12

## Scope

Continuation of `rainier` DNS/Caddy/Route53 work and cleanup verification.

## Key outcomes

- Route 53 DNS-01 issuance for `dns.blackridge.shumie.net` succeeded and a public Let's Encrypt cert is now active.
- Caddy now runs from a custom build with the Route 53 DNS provider module.
- ACME propagation required explicit resolvers in the Caddy TLS block and container DNS overrides to public resolvers.
- Caddy-to-AdGuard proxy for `/dns-query` works when targeting `192.168.3.10:3000` (not `host.docker.internal`).
- DoH endpoint validated from macOS using `kdig`:
  - `kdig @dns.blackridge.shumie.net +https +short cloudflare.com A` succeeds.
- Post-reboot checks on `rainier` succeeded:
  - all containers up
  - plain DNS works
  - `/dns-query` reachable
  - Home Assistant reachable via Caddy
  - `ufw` rules persisted
- Teleport VPN DNS/SSH issue resolved:
  - root cause: `ufw` only allowed `53/tcp`, `53/udp`, and `22/tcp` from `192.168.3.0/24`
  - Teleport clients arrive from `192.168.2.0/24`, so DNS and SSH were blocked over VPN
  - fix: allow `192.168.2.0/24` to `53/tcp`, `53/udp`, and `22/tcp`
  - outcome: hostnames resolve over VPN via AdGuard (`192.168.3.10`) and SSH works over Teleport

## Current live configuration facts

- Caddy compose uses:
  - custom Dockerfile build with `caddy-dns/route53`
  - env file `.env.route53` (local-only)
  - container DNS set to `1.1.1.1` and `9.9.9.9`
- Caddyfile for `dns.blackridge.shumie.net`:
  - `tls` with `dns route53 { hosted_zone_id $ROUTE53_HOSTED_ZONE_ID }`
  - `resolvers 1.1.1.1 9.9.9.9`
  - `reverse_proxy 192.168.3.10:3000`
- Route 53 hosted zone ID for `shumie.net`: `Z1XIAIXXJ9KA3O`

## Firewall exceptions required

- `172.18.0.0/16 -> 3000/tcp` (Caddy to AdGuard)
- `172.18.0.0/16 -> 8123/tcp` (Caddy to Home Assistant)
- `172.17.0.0/16 -> 53/tcp,53/udp` (Docker build containers to AdGuard DNS)

## Outstanding follow-ups

- Decide whether to deploy Apple DNS profiles to use `https://dns.blackridge.shumie.net/dns-query`.
- Implement a real backup job for stateful data and volumes.
